import React from 'react';

type MovieCardProps = {

};

const MovieCard: React.FC<MovieCardProps> = ({

}) => {
    return (
        <div className='movie-card' style={{ border: '1px solid #ddd', borderRadius: 8, padding: 16, maxWidth: 300 }}>
            
        </div>
    );
};

export default MovieCard;